import Codeblock from "@/components/Codeblock";

# Standar Deviasi

Rumus:

```
σ = √∑(x - μ)^2 / n
```

Fungsi [sumber kode [disini](https://github.com/bellshade/OpenSeries/blob/main/OpenSeries/statistika.py#L27)]

```python
def standar_deviasi(
    vektor: np.ndarray
) -> Union[float, str]:
```

Contoh Kode

```python
import numpy as np
from OpenSeries import statistika as statistika

vektor = np.array([1, 2, 3, 4, 5])
hasil = statistika.standar_deviasi(vektor)
print(hasil)
```

## Coba Sekarang

<Codeblock code={`import numpy as np
from OpenSeries import statistika as statistika

vektor = np.array([1, 2, 3, 4, 5])
hasil = statistika.standar_deviasi(vektor)
print(hasil)
`} />
